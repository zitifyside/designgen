# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Settings / validation ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# --- Database (async SQLAlchemy, SQLite by default, swap to Postgres via DATABASE_URL) ---
SQLAlchemy[asyncio]==2.0.36
aiosqlite==0.20.0
alembic==1.14.0
# Uncomment for PostgreSQL:
# asyncpg==0.30.0

# --- Auth / security ---
PyJWT==2.10.1
bcrypt==4.2.1
pyotp==2.9.0            # TOTP (2FA)

# --- AI providers (used by app/services/ai — actual prompts are left empty) ---
# Gemini:
google-genai==0.8.0
# Codex / OpenAI:
openai==1.59.6

# --- Misc ---
httpx==0.28.1
pypdf==5.1.0
